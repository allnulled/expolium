module.exports = (runner) => {
	return new Promise((resolve, reject) => {
		const path = require("path");
		const {
			assert,
			expect
		} = require("chai");
		describe("RunnerTest", () => {
			let FormBuilder = undefined;
			it("can start project", () => {});
		});
		resolve();
	});
}