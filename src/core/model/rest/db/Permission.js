const BasePermission = require(process.env.PROJECT_ROOT + "/core/model/rest/db/base/BasePermission.js");

class Permission extends BasePermission {
    // @Define your own model here
    // @Remember that this file is only updated manually
}

module.exports = Permission;
