const BaseSession = require(process.env.PROJECT_ROOT + "/core/model/rest/db/base/BaseSession.js");

class Session extends BaseSession {
    // @Define your own model here
    // @Remember that this file is only updated manually
}

module.exports = Session;
