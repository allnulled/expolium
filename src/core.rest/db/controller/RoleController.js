const BaseRoleController = require(process.env.PROJECT_ROOT + "/core.rest/db/controller/base/BaseRoleController.js");

class RoleController extends BaseRoleController {
    // @Define your own controller here
    // @Remember that this file is only updated manually
}

module.exports = RoleController;
