const BaseSessionController = require(process.env.PROJECT_ROOT + "/core.rest/db/controller/base/BaseSessionController.js");

class SessionController extends BaseSessionController {
    // @Define your own controller here
    // @Remember that this file is only updated manually
}

module.exports = SessionController;
