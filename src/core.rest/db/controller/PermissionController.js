const BasePermissionController = require(process.env.PROJECT_ROOT + "/core.rest/db/controller/base/BasePermissionController.js");

class PermissionController extends BasePermissionController {
    // @Define your own controller here
    // @Remember that this file is only updated manually
}

module.exports = PermissionController;
