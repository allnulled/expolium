const BaseUser = require(process.env.PROJECT_ROOT + "/core.rest/db/model/base/BaseUser.js");

class User extends BaseUser {
    // @Define your own model here
    // @Remember that this file is only updated manually
}

module.exports = User;
