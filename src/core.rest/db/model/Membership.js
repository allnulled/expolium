const BaseMembership = require(process.env.PROJECT_ROOT + "/core.rest/db/model/base/BaseMembership.js");

class Membership extends BaseMembership {
    // @Define your own model here
    // @Remember that this file is only updated manually
}

module.exports = Membership;
